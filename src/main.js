import "@/styles/main.scss";

// main.js
document.addEventListener('DOMContentLoaded', () => {
  console.log('App cargada');
  // inicializaciones, listeners, import dinámico de módulos si conviene
});
